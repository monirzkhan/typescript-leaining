//utilities types

//Utility types are built-in types in TypeScript that provide common type transformations and manipulations. 
// They help developers to create new types based on existing ones, making it easier to work with complex data 
// structures and improve code readability.

type User = {
    id: number;
    name: string;
    email: string;
    age: number;
    isAdmin: boolean;   
}   

